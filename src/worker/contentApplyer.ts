// 制作和插入面包屑

import { type IProtyle } from "siyuan";
import { getPluginInstance } from "@/utils/pluginInstance";
import { logLog, logDebug } from "@/utils/logger";

export default class ContentApplyer {

    constructor() {
    }

    apply(protyle: IProtyle) {
        // 检查是否已经插入

        // 构建容器

        // 构建面包屑

        // 构建相邻文档

    }
}